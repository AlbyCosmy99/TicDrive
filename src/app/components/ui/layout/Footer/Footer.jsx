import styles from './footer.module.css'

export default function Footer() {
    return (
        <div className={`${styles.footerBackground} d-flex justify-content-center`}>
            <b>Support</b>
        </div>
    )
}